# Compiler

This repository contains a simple parser written in Bison (`parser.y`) for an
arithmetic expression language with variables and assignments. The parser uses a
minimal symbol table implemented in `symbol_table.c`/`symbol_table.h` to store
values of variables encountered while parsing.
